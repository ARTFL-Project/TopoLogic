.DS_Store
node_modules
/dist

# Per-deployment files — the dev tree may carry stubs that shouldn't be committed.
appConfig.json
appConfig.build.json
model_config.ini
topic_words.json

# local env files
.env.local
.env.*.local

# Log files
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Editor directories and files
.idea
.vscode
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
